<div id="message">
    <% if !message.user.nil? %>
        <div id="messageHeader"><p><strong><%= link_to User.find(message.user).username, user_path(message.user) %> </strong>
            <span id="timeStamp"><%=message.created_at.strftime("%I:%M %p")%></span></p>
        </div>
        <div id="messageContent"><%=message.content%></div>
    <% else %>
        <div id="messageHeader"><p><strong>Gerbil </strong>
            <span id="timeStamp"><%=message.created_at.strftime("%I:%M %p")%></span></p>
        </div>
        <div id="messageContent"><%=message.content%></div>
    <% end %>
</div>
